#include <stdio.h>

  double f(double x){
  return (pow(x,3))/((4 * pow(x,4))+10);
    }
int main(void) {
double a=1,b=10,n=10000;
  double result = 0, h = (b - a) / n;
  
  for(int i = 0; i < n; i++) {
    result += h* f(a+i*h);
  }
  printf("%d",result);
  return 0;
}
